services:

  # ── MQTT Broker ─────────────────────────────────────────────────────────
  mosquitto:
    image: eclipse-mosquitto:2.0
    container_name: mosquitto
    volumes:
      - ./mosquitto/config/mosquitto.conf:/mosquitto/config/mosquitto.conf
      - mosquitto_data:/mosquitto/data
    networks:
      - iot_net
    restart: unless-stopped

  # ── Time-series Database (SV2) ──────────────────────────────────────────
  influxdb:
    image: influxdb:2.7
    container_name: influxdb
    environment:
      DOCKER_INFLUXDB_INIT_MODE: setup
      DOCKER_INFLUXDB_INIT_USERNAME: ${INFLUXDB_USERNAME:-admin}
      DOCKER_INFLUXDB_INIT_PASSWORD: ${INFLUXDB_PASSWORD:-admin12345}
      DOCKER_INFLUXDB_INIT_ORG: ${INFLUXDB_ORG:-hust}
      DOCKER_INFLUXDB_INIT_BUCKET: ${INFLUXDB_BUCKET:-iot}
      DOCKER_INFLUXDB_INIT_ADMIN_TOKEN: ${INFLUXDB_TOKEN:-dev-token-change-me}
    volumes:
      - influxdb_data:/var/lib/influxdb2
    networks:
      - iot_net
    restart: unless-stopped

  # ── Edge IoT Gateway (SV2) ──────────────────────────────────────────────
  waste-gateway:
    build: ./iot_gateway
    container_name: waste-gateway
    environment:
      GATEWAY_ID: waste-gateway
      MQTT_BROKER: mosquitto
      MQTT_PORT: 1883
      INFLUXDB_URL: http://influxdb:8086
      INFLUXDB_TOKEN: ${INFLUXDB_TOKEN:-dev-token-change-me}
      INFLUXDB_ORG: ${INFLUXDB_ORG:-hust}
      INFLUXDB_BUCKET: ${INFLUXDB_BUCKET:-iot}
      # Ngưỡng rule engine — chỉnh được không cần sửa code
      FILL_DISPATCH_THRESHOLD: 85
      FILL_CRITICAL_THRESHOLD: 95
      TEMP_FIRE_THRESHOLD: 60
      METHANE_ALERT_THRESHOLD: 500
      WEIGHT_LOCK_THRESHOLD: 60
      SENSOR_OFFLINE_TIMEOUT: 30
      COLLECTION_DELAY: 20
    depends_on:
      - mosquitto
      - influxdb
    networks:
      - iot_net
    restart: unless-stopped

  # ── Virtual Sensors ─────────────────────────────────────────────────────
  sensor-bin-01:
    build: ./virtual_sensor
    container_name: sensor-bin-01
    environment:
      AREA_ID: district-1
      BIN_ID: bin-01
      DEVICE_ID: sensor-bin-01
      MQTT_BROKER: mosquitto
      MQTT_PORT: 1883
      PUBLISH_INTERVAL: 5
    depends_on:
      - mosquitto
    networks:
      - iot_net
    restart: unless-stopped

  sensor-bin-02:
    build: ./virtual_sensor
    container_name: sensor-bin-02
    environment:
      AREA_ID: district-1
      BIN_ID: bin-02
      DEVICE_ID: sensor-bin-02
      MQTT_BROKER: mosquitto
      MQTT_PORT: 1883
      PUBLISH_INTERVAL: 5
    depends_on:
      - mosquitto
    networks:
      - iot_net
    restart: unless-stopped

  sensor-bin-03:
    build: ./virtual_sensor
    container_name: sensor-bin-03
    environment:
      AREA_ID: district-1
      BIN_ID: bin-03
      DEVICE_ID: sensor-bin-03
      MQTT_BROKER: mosquitto
      MQTT_PORT: 1883
      PUBLISH_INTERVAL: 7
    depends_on:
      - mosquitto
    networks:
      - iot_net
    restart: unless-stopped

  # ── Virtual Actuators ───────────────────────────────────────────────────
  actuator-bin-01:
    build: ./virtual_actuator
    container_name: actuator-bin-01
    environment:
      AREA_ID: district-1
      BIN_ID: bin-01
      DEVICE_ID: actuator-bin-01
      MQTT_BROKER: mosquitto
      MQTT_PORT: 1883
    depends_on:
      - mosquitto
    networks:
      - iot_net
    restart: unless-stopped

  actuator-bin-02:
    build: ./virtual_actuator
    container_name: actuator-bin-02
    environment:
      AREA_ID: district-1
      BIN_ID: bin-02
      DEVICE_ID: actuator-bin-02
      MQTT_BROKER: mosquitto
      MQTT_PORT: 1883
    depends_on:
      - mosquitto
    networks:
      - iot_net
    restart: unless-stopped

  actuator-bin-03:
    build: ./virtual_actuator
    container_name: actuator-bin-03
    environment:
      AREA_ID: district-1
      BIN_ID: bin-03
      DEVICE_ID: actuator-bin-03
      MQTT_BROKER: mosquitto
      MQTT_PORT: 1883
    depends_on:
      - mosquitto
    networks:
      - iot_net
    restart: unless-stopped

volumes:
  mosquitto_data:
  influxdb_data:

networks:
  iot_net:
    driver: bridge
